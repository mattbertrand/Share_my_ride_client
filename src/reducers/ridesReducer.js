const initialState = {
    rides: [],
    loading: true
}

const ridesReducer = (state, action) => {
    switch(action.type) {
        default:
            return state
    }
}

export default ridesReducer